name: CI/CD Pipeline

on:
  push:
    branches:
      -main


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.9'

    
    - name: Install dependencies
      run: |

    - name: Build and Deploy to Heroku
      env:
        HEROKU_API_KEY: ${{HRKU-cc4a3c06-7f9a-4975-8ba0-78ca9bc2bf04}}
      run: |